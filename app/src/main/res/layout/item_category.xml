<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/half_main_padding"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    card_view:cardCornerRadius="@dimen/corner_radius_header"
    card_view:cardElevation="@dimen/card_elevation">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/ivCategoryImage"
            android:layout_width="match_parent"
            android:layout_height="@dimen/card_height_of_categories"
            android:contentDescription="@string/card_category"
            android:scaleType="centerCrop"
            tools:src="@drawable/burger" />

        <TextView
            android:id="@+id/tvCategoryTitle"
            style="@style/CardTitleText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/half_main_padding"
            android:layout_marginTop="@dimen/half_main_padding"
            tools:text="@string/tools_card_category" />

        <TextView
            android:id="@+id/tvCategoryDescription"
            style="@style/CardDescriptionText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="@dimen/half_main_padding"
            android:layout_marginBottom="@dimen/header_title_indents"
            android:layout_marginTop="@dimen/half_main_padding"
            tools:text="@string/tools_card_description_category" />

    </LinearLayout>

</androidx.cardview.widget.CardView>