<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/categories_fragment_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/backgroundColor"
    android:src="@android:color/transparent"
    android:orientation="vertical">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/headerContainer"
        android:layout_width="match_parent"
        android:layout_height="@dimen/header_height_224">

        <ImageView
            android:id="@+id/ivRecipeImage"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@string/content_description_image_recipes"
            android:scaleType="centerCrop"
            android:src=""
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <TextView
            android:id="@+id/tvCategoryName"
            style="@style/ListRecipesBurgerText"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/main_padding_16"
            android:layout_width="wrap_content"
            android:background="@drawable/share_rounded_screen_header"
            android:padding="@dimen/main_padding_indent_10"
            android:textColor="@color/headerTitleColor"
            app:layout_constraintBottom_toBottomOf="@+id/ivRecipeImage"
            app:layout_constraintStart_toStartOf="parent"
            tools:text="Бургеры"/>
    </androidx.constraintlayout.widget.ConstraintLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvRecipes"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_margin="@dimen/half_main_padding_8"
        tools:itemCount="8"
        tools:LinearLayoutManager="androidx.recyclerview.widget.GridLayoutManager"
        tools:listitem="@layout/item_recipe"
        tools:spanCount="1"/>

</LinearLayout>